Markdown to LaTeX with Pandoc. 

Usage: mdpdf <mdfile> [-t template] [options]

templates: 
	pl|plain (default)
	gr|gradecomments
	sy|syllabus
	la|labmanual

options:
	-z		render 中文 (use xelatex, set CJKfont, etc)
	-c 		add table of contents (--toc)
