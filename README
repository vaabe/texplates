Usage: mdpdf <mdfile> [options]

options:
	-t		template (below)
	-z		render 中文 (use xelatex, set CJKfont, etc)
	-c 		add table of contents 
	-x 		save .tex file (create ./tex/ directory)

templates: 
	pl|plain (default)
	no|note
	gr|gradecomments
	sy|syllabus
	la|labmanual

