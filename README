Some templates to convert markdown to LaTeX. With Pandoc.  

Usage: mdpdf <mdfile> [-t template] [-z] [-c]

templates: 
	plain (default)
	grade|gradecomments
	syl|syllabus
	lab|labmanual

options:
	-z 		add 中文 support (use xelatex, set CJKfont, etc)
	-c 		force add table of contents (--toc)
